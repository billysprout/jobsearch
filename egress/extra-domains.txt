# Widened allowlist — ships EMPTY (agent must never have npm egress).
# `bash setup.sh egress-setup` writes registry.npmjs.org here for scripted
# operator steps; `bash setup.sh egress-lockdown` empties it again.

# WhatsApp channel (linked-device transport) — added 2026-08-23, see SECURITY-REVIEW §3 row 15
.whatsapp.net
.whatsapp.com

# Baileys (WhatsApp plugin transport) fetches its protocol version from GitHub raw
# on every connect/reconnect, not just first login; a denied fetch falls back to a
# bundled version that WhatsApp's servers reject outright, causing a reconnect loop.
# No ssl_bump here, so squid can't scope this to a path — exact host only (not a
# wildcard suffix) to keep it as tight as this proxy allows. See SECURITY-REVIEW §3 row 15.
raw.githubusercontent.com

# --- Telegram channel (second, additional channel), prep started 2026-08-24 ---
# NOT YET LIVE — no bot token exists yet (create via @BotFather first).
# Added now so the egress leg is ready when it is. See SECURITY-REVIEW.md §4 R16-R18.
# (A Meta WhatsApp Cloud API migration was tried first and abandoned same-day —
# see SECURITY-REVIEW.md §4 R15 — this replaces that plan, not the WhatsApp
# channel above, which is untouched.)

# Telegram Bot API — long polling (default transport, no inbound needed) and
# outbound sends both go through this single documented endpoint. Exact host,
# no wildcard.
api.telegram.org

# --- Salary research (mcp-salary container), added 2026-09 ---
# BLS public data API — official OEWS wage statistics, no auth, GET/POST only.
# Exact host only (download.bls.gov deliberately NOT allowed — the MCP tool
# links it for humans but doesn't fetch it).
api.bls.gov